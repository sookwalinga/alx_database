-- This SQL script creates the database hbtn_0c_0 if it doesn't already exist.

-- Check if the database hbtn_0c_0 already exists
-- The query tries to create the database, if it already exists, it'll not fail due to IF NOT EXISTS clause
CREATE DATABASE IF NOT EXISTS hbtn_0c_0;
