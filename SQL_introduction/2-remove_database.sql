-- This SQL script deletes the database hbtn_0c_0 if it exists.

-- Try to drop (delete) the database hbtn_0c_0
-- The query will attempt to drop the database, and if it doesn't exist, the query will not fail due to the IF EXISTS clause
DROP DATABASE IF EXISTS hbtn_0c_0;
