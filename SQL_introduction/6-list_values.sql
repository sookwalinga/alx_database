-- This SQL script lists all rows of the table first_table from the specified database.

-- The database name is passed as an argument to the mysql command
-- The user is prompted to enter the password for authentication
-- Replace "database_name" with the actual name of the database containing the table
SELECT *
FROM database_name.first_table;
